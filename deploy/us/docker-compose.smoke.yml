services:
  qwen2api:
    container_name: qwen2api-smoke
    restart: "no"
    ports: !override
      - "${SMOKE_HOST_PORT:-17860}:${PORT:-7860}"
    volumes: !override
      - /tmp/qwen2api-smoke-data:/app/data
      - /tmp/qwen2api-smoke-logs:/app/logs
